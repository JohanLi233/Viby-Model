[project]
name = "viby"
version = "0.1.0"
description = "Viby: a small dense LLM (pretrain / SFT / DPO) built on Apple MLX"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1.0",
    "mlx>=0.32.0",
    "swanlab>=0.9.4",
    "torch>=2.13.0",
    "transformers>=4.54.1",
    "zhconv>=1.4.3",
]
