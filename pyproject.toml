[project]
name = "viby"
version = "0.1.0"
description = "Viby: DeepSeek-V4.1 architecture (CED + CSA2 + mHC + MoE + Engram + DSpark) scaled down, built on Apple MLX"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1.0",
    "matplotlib>=3.11.1",
    "mlx>=0.32.0",
    "swanlab>=0.9.4",
    "torch>=2.13.0",
    "tqdm>=4.67.0",
    "transformers>=4.54.1",
    "zhconv>=1.4.3",
]
